[project]
name = "codigo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
# The saved inverse-kinematics models use the Keras 2.13 serialization format.
# TensorFlow 2.13 supports Python 3.8 through 3.11; Python 3.9 is the
# recommended inference runtime for this repository.
requires-python = ">=3.8,<3.12"
dependencies = [
    "jupyter>=1.1.1",
    "keras==2.13.1",
    "matplotlib>=3.7.5",
    "ndicapi>=3.7.6",
    "pandas>=2.0.3",
    "plotly>=6.9.0",
    "pyserial>=3.5",
    "scikit-surgerynditracker>=1.0.1",
    "scipy>=1.10.1",
    "seaborn>=0.13.2",
    "serial>=0.0.97",
    "tensorflow==2.13.1",
]
